---
- name: Run show ip int brief on Cisco devices
  hosts: cisco
  gather_facts: no
  
  tasks:
    - name: Execute show ip int brief command
      ansible.netcommon.cli_command:
        command: show ip int brief
      register: output

    - name: Display command output
      ansible.builtin.debug:
        msg: "{{ output.stdout_lines }}"